<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Revelación del Bebé</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
      background: #fefefe;
      padding: 40px;
    }
    h1 {
      font-size: 2.5em;
      color: #444;
    }
    #reveal {
      margin-top: 40px;
      padding: 20px 40px;
      font-size: 1.5em;
      background: #4b0082;
      color: white;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      transition: background 0.3s;
    }
    #reveal:hover {
      background: #6a0dad;
    }
    #result {
      margin-top: 50px;
      font-size: 2.5em;
      display: none;
    }
    .confetti {
      position: fixed;
      width: 10px;
      height: 10px;
      background-color: pink;
      animation: fall 3s linear infinite;
      top: 0;
      left: 50%;
    }
    @keyframes fall {
      to {
        transform: translateY(100vh) rotate(360deg);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <h1>¡La Gran Revelación Está por Llegar!</h1>
  <button id="reveal">Haz clic para revelar 💖</button>
  <div id="result"></div>

  <script>
    const button = document.getElementById('reveal');
    const result = document.getElementById('result');
    let revealed = false;

    button.addEventListener('click', () => {
      if (revealed) return;

      // Cambia esto a "niño" o "niña"
      const sexo = "niña";

      result.style.display = "block";
      result.textContent = `¡Es una ${sexo}! 💕`;
      revealed = true;

      // Crear confeti
      for (let i = 0; i < 100; i++) {
        let confetti = document.createElement('div');
        confetti.className = 'confetti';
        confetti.style.left = Math.random() * 100 + 'vw';
        confetti.style.backgroundColor = sexo === "niña" ? "pink" : "skyblue";
        document.body.appendChild(confetti);
        setTimeout(() => confetti.remove(), 3000);
      }
    });
  </script>
</body>
</html>
